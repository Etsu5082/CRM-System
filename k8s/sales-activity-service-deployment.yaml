apiVersion: apps/v1
kind: Deployment
metadata:
  name: sales-activity-service
  namespace: crm-system
  labels:
    app: sales-activity-service
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: sales-activity-service
  template:
    metadata:
      labels:
        app: sales-activity-service
        version: v1
    spec:
      containers:
      - name: sales-activity-service
        image: crm/sales-activity-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3102
          name: http
        env:
        - name: PORT
          value: "3102"
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: crm-config
              key: NODE_ENV
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: crm-secrets
              key: SALES_DB_URL
        - name: CUSTOMER_SERVICE_URL
          value: "http://customer-service:3101"
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: crm-config
              key: KAFKA_BROKERS
        - name: KAFKA_CLIENT_ID
          value: "sales-activity-service"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3102
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3102
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: sales-activity-service
  namespace: crm-system
  labels:
    app: sales-activity-service
spec:
  selector:
    app: sales-activity-service
  ports:
  - protocol: TCP
    port: 3102
    targetPort: 3102
    name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sales-activity-service-hpa
  namespace: crm-system
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sales-activity-service
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
