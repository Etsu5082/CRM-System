apiVersion: apps/v1
kind: Deployment
metadata:
  name: opportunity-service
  namespace: crm-system
  labels:
    app: opportunity-service
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: opportunity-service
  template:
    metadata:
      labels:
        app: opportunity-service
        version: v1
    spec:
      containers:
      - name: opportunity-service
        image: crm/opportunity-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3103
          name: http
        env:
        - name: PORT
          value: "3103"
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: crm-config
              key: NODE_ENV
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: crm-secrets
              key: OPPORTUNITY_DB_URL
        - name: CUSTOMER_SERVICE_URL
          value: "http://customer-service:3101"
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: crm-config
              key: KAFKA_BROKERS
        - name: KAFKA_CLIENT_ID
          value: "opportunity-service"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3103
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3103
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: opportunity-service
  namespace: crm-system
  labels:
    app: opportunity-service
spec:
  selector:
    app: opportunity-service
  ports:
  - protocol: TCP
    port: 3103
    targetPort: 3103
    name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: opportunity-service-hpa
  namespace: crm-system
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: opportunity-service
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
